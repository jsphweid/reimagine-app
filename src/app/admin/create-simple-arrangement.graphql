mutation CreateSimpleArrangement(
  $pieceId: String!
  $name: String!
  $base64Blob: String!
) {
  createSimpleArrangement(
    pieceId: $pieceId
    name: $name
    base64Blob: $base64Blob
  ) {
    id
    name
    pieceId
    dateCreated
  }
}
