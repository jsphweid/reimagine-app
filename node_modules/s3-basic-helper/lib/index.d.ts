export default class S3BasicHelper {
    private s3Client;
    private region;
    private profile;
    private additionalS3ClientConfig;
    constructor(region: string, profile?: string, additionalS3ClientConfig?: {});
    private getAwsCreds();
    private maybeSetUpClient();
    private getFilesInBucketRoot(bucket);
    private deleteObjects(bucketName, arrOfObjectsToDelete);
    uploadDir(bucketName: string, localDir: string, remoteDir: string, deleteRemoved: boolean): Promise<void>;
    uploadSafelyToRootDir(bucketName: string, localDir: string): Promise<void>;
}
